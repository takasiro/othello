#include <stdio.h>
#include "AllJudge.h"

/************************************************
関 数 名 :AllJudge(const int * ,int player )
処理内容 :どこかに駒が置けるかどうかの判定
引    数 :無し
返 却 値 :無し
備　　考 :1：置ける
　　　　　0：置けない　
	 　　　　PassJudge();
*************************************************/
void AllJudge() {
	int Board[10][10];
	//Board = SetMasu();
	//returnの値で管理する
	//TRUE PieceReverse();
	//FALSE PassJudge();

	//マウス座標より要素数を割り出し配列へ代入
	//mousei = pt.x / 50 % 8 + 1;
	//mousej = pt.y / 50 % 8 + 1;
}